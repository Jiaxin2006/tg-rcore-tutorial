## 一、与AI合作的实现过程

1. 如何与AI交互

我写代码的时候大部分时间使用 cursor 的 auto 模型，prompt 中我倾向于按条列出我希望它做的事情，这样它会给自己列一个 todo list 并完成，比纯粹的 chat 更不容易漏点。同时，虽然 context length 没有超，但是模型也会遗忘，需要自己把需要模型注意的点强调出来。ch5 之后我发现 auto 比较笨，于是换成了 GPT-5.3 Codex。不过模型太聪明了也有缺点 —— 会丧失一些从它的错误中学习的机会。

cursor 相比部分 coding 工具的一个好处是它会展示出修改了哪些部分并可以回退。因此在赶时间的时候能够清晰地看出任务的主干代码是什么；有时间的时候也可以把修改部分删掉，重写一遍函数功能，并和 ai 的实现做对应，以此练习自己对操作系统架构和rust语法的掌握。在完成之后，我还会请 ai 给我针对这次实验提几个问题，以检查自己知识层面的理解。

在做 T2 时，我会先把飞书上的题目 + 我自己对实现的理解（功能的具体定义等）输入给AI，请它帮我添加一个定义和函数框架；随后，我再手动检查框架的定义是否合理，并逐个与 AI 协同实现函数。常常，我发现 AI 会偷懒，比如现有接口能够得到一个可能说通但其实并不正确的功能调用，它就不会实现新的接口，于是需要人工审核 + 调整。

T2L4 要嵌入内核需要实现的内容太多了，考虑到同学的实现难度需要与其他实验平齐并能够实现、比较更多算法，我要求AI实现了一个模拟器，测试的时候用真实情境的代表任务。

在做 T3 的时候，我发现直接把所有信息都提供给 AI 并期待它直接进行实现，由于 demo 的复杂性和 coding 模型在多模态能力（处理图像信息）的局限性(即使是从前无往而不利的 Opus 也不行)，会导致极长的思考过程，以至于无论从模型生成结果的可靠性还是从我自己的 token 余量来说都不可行。

2. 碰到的问题/bug

ch3: 出现了两处语法 bug：一个是带 unsafe 的类型转换没有加括号，一个是函数中没有直接把id 声明成 SyscallId 导致编译器推断的时候目标类型不明确。以及我发现syscall 使用静态表存储会超出内核栈。
打包的时候发现因为编译环境不一样导致出现了 未使用变量 的错误。

ch4: 再次出现了 unsafe 类型转换没加括号的问题（我并没有开新的对话，说明大模型的长上下文信息追溯能力还是有问题）；而且大模型自动了理解为syscall 调用次数的统计不再需要因此没有实现，且在我让他实现的时候实现的还是 内核cache 会爆炸的版本。打包的时候又出现了变量未使用错误，发现只要有只在 exercise 下用到的代码就会报类似错误，每次都改比较麻烦。

ch5-6: 这两次我给 ai 的 prompt 中加上了过去已经出现过的 bug 提示，以避免它无法 retrieve 过去的聊天记录并吸取教训。整体没碰到什么 bug。

ch6 和 ch8: 打包的时候遇到了 Cargo.toml 文件中依赖的仍是过去版本的 efs/sync，导致crates 上传的时候编译报 method not found.

T2L4: starvation 的统计，由于 stats 里面实现了总 waiting 时间，模型自动生成的代码就直接调用了这个数据，而不是比较当次 waiting 时间和 threshold。

T2L5: 我发现 mutex release 的时候没有先把 locked 参数释放掉，被唤醒的线程又重新入队等待，导致陷入死锁。而且，尽管我给了 prompt，AI 自动实现的测试任务无法满足失败情况的监测。

T3L1: 一开始我尝试先把 VirtIO-GPU 和 framebuffer 接口搭好，再让模型根据参考图自动推断七巧板的摆放方式。但由于当前 coding 模型对图像的几何关系理解并不稳定，它更容易给出“颜色大致对、比例和位置不对”的结果；如果继续让它黑箱式自动调整，不但 token 消耗很大，而且很难判断每次修改到底是否更接近目标。

T3L2: Rust 2024 不再允许直接创建 static mut 的可变引用（或任何引用），而必须通过原始指针来操作。导致之前的代码出错。

3. 解决过程

ch3: 语法错误直接把 bug 描述喂给 ai 就能解决（不过错的比较低级，手动修或许更快）。ai 写代码的时候不会考虑太多复杂情况和边界条件，导致栈溢出这类错误还是要靠人来发现。

ch4: 手动修改括号，并从 ch3 的代码 merge。发布到 crates 时添加 `--features exercise`.

ch6/8: 打包对应的依赖组件，也上传到 crates 上。

T2L4: 增加保存上次状态改变时间，以便统计。

T2L5: 手动推演了一遍锁逻辑并要求 AI 修改。把想要设计的实验逻辑也告诉 AI，使它可以帮我写出检测出错的test。

T3: 请它先帮我规划需要怎么实现，我审核后请它一个一个实现对应的函数代码，这样既有利于 code review，也使得有些部分（比如七巧板的摆放）能够由我来更方便地调整，而不是由它直接实现出来。

T3L1: 后来我先让 AI 规划并逐步实现底层部分：QEMU 图形 runner、VirtIO-GPU 初始化、framebuffer 写入与 flush、最小栅格化函数。七巧板摆放本身则改成“人审阅 + AI 协助改坐标”的方式：一度尝试过“标准七巧板积木 + 定点坐标旋转”的参数化模型，但它对这道题反而过于复杂，相当于把“摆图”问题套成了两层变换。最后回到更直接的方案：在 `tangram.rs` 中用 `Piece::Tri` / `Piece::Quad` 和顶点数组直接描述参考图中的每一块，并额外保留 `render_piece` / `render_progressive` 等接口，方便后续 ch2 做逐块显示。

T3L2: static mut 是全局可变静态变量，为了避免直接获取可变应用带来的数据竞争风险，现在使用原始指针，然后 addr_of_mut! 获取地址再解引用。

T3L2: 在把 ch1 的图形能力接到 ch2 时，我一开始低估了“向前兼容”的难度。因为 ch2 原本只是串口输出 + 批处理 + `write/exit` syscall，而这次扩展额外引入了 GPU、heap、DMA pool、本地 tg-user 用例修改，以及 13 个专门用于逐块渲染的用户程序。如果直接按“做出效果”来改，很容易破坏原有基础测试，因此后面我把目标重新收敛成两条并行约束：一是保持 `./test.sh` 继续通过，二是在测试用例之后追加 tangram 动画。

T3L2: 具体实现上，我没有额外扩展共享的 syscall crate，而是复用了现有 `write` 路径，把 `fd = 100` 约定成“绘图请求”。这样用户程序只需要 `write(100, &[piece_id])`，内核在 `SyscallContext::write()` 里识别这个特殊 fd，再调用 `tangram_draw(piece_id)`，由它把 `piece_id` 转成 `(o_count, s_count)` 并复用 `render_progressive()` 重绘 framebuffer。这个方案的好处是：它不破坏原有 ch2 的 syscall 教学主线，也避免了为了一个实验专门改共享接口。

T3L2: 真正棘手的问题反而出在内存布局上。最初 tangram 用户程序虽然能把一块画出来，但之后会因为 `IllegalInstruction` 被杀掉。排查后发现并不是用户程序自身逻辑错了，而是我新增的 GPU DMA pool 和 heap 把用户程序装载地址附近的内存覆盖了。最终把 `cases.toml` 中 `[ch2]` 的 `base` 从 `0x8040_0000` 上移到 `0x8400_0000` 后，13 个 tangram app 才都能在绘制完自己那一块后正常 `exit`。这次经历让我意识到：系统实验里很多 bug 看起来像“指令异常”，本质却是内存布局冲突。

## 二、T1 学习效果评估

1. 自己在这个学习过程中知识和能力的提升/下降评价

- 和 ai 合作的时候能从它的思考过程中学到很多 检查代码结构、debug 的方法。
- 整体实验内容和校内小实验差别不大，思想都很一致，所以更像是一遍复习。

1. 与本校现有教学实验教程的定量/定性的对比分析

- 完成时间：现有教学实验我的完成节奏大约是3天一个 Chapter；而这次的实验我大概三天就全部完成了。
- 难度体会：不知道是不是和做过了本校小实验有关，感觉这一版实现起来好像容易一些。从修改行数来看，代码量基本差不多。对大部分同学的水平而言，我猜想框架的差别对难度的影响远不如学 Rust 语言带来的难度。

## 三、T2L4 学习效果评估

### 1. 定量指标

**覆盖度对比**


| 维度   | 原实验（ch3-ch8）                     | 我的扩展实验（exp4）                                                                              |
| ---- | -------------------------------- | ----------------------------------------------------------------------------------------- |
| 调度算法 | FIFO/RR + stride（ch5 练习）         | FCFS / SJF / RR / MLFQ / CFS-like（5 种）                                                    |
| 调度接口 | `Schedule { add, fetch }`（2 个方法） | `PluggableScheduler { enqueue, pick_next, on_tick, on_block, on_wakeup, on_exit }`（6 个方法） |
| 可观测性 | 无统一指标，只看"用户程序能否跑通"               | 统一 `MetricsCollector` 采集 7 种事件，输出 6 项指标                                                   |
| 测试体系 | 功能验收（`test.sh`，检查输出字符串）          | 脚本化 workload × 3 组 + 自动对比表 + 可重复运行                                                        |


**性能/行为对比（exp4 Mixed workload 实测数据）**


| 指标       | FCFS   | SJF    | RR-10  | MLFQ-3     | CFS    |
| -------- | ------ | ------ | ------ | ---------- | ------ |
| 平均等待时间   | 72.60  | 69.80  | 61.80  | **36.20**  | 38.40  |
| 平均周转时间   | 110.80 | 108.00 | 98.20  | **72.80**  | 72.40  |
| 吞吐量      | 0.0329 | 0.0360 | 0.0407 | **0.0407** | 0.0407 |
| 交互延迟 P95 | 43     | 56     | 23     | **7**      | 9      |
| 饥饿次数     | 4      | 4      | 0      | **0**      | 0      |


关键结论：

- MLFQ 和 CFS 在混合负载下表现最优，交互延迟 P95 从 FCFS 的 43 降至 7（降幅 84%），饥饿完全消除
- SJF 的 P95 反而比 FCFS 更差（56 vs 43），因为它按总 burst 排序，交互型短任务被长任务的"短估计"挤占
- 原实验的 stride 调度无法直接比较（它解决的是"按优先级比例分配 CPU"，而非"减少交互延迟"）
- 后续又补做了真实替换验证：`tg-rcore-tutorial-ch8` 的线程调度管理器已切到 `exp4-scheduler::DefaultTaskManager`，并通过 `cargo check`，说明 `exp4` 已经不仅是模拟器，而是能保持默认 FIFO 行为不变地替换旧 scheduler

**原实验 vs 新实验的接口对比**


|         | 原 `Schedule`       | 新 `PluggableScheduler`                   |
| ------- | ------------------ | ---------------------------------------- |
| 时钟中断感知  | 无（外部硬编码抢占）         | `on_tick` 回调，策略自主决定是否抢占                  |
| 阻塞/唤醒感知 | 无（外部直接 add/remove） | `on_block/on_wakeup`，策略可调整优先级            |
| 退出清理    | 无                  | `on_exit`，CFS 可清理 vruntime               |
| 时间戳     | 无                  | 所有方法接收 `now: Tick`                       |
| 数据采集    | 无                  | 统一 `SchedEvent` 事件流 + `MetricsCollector` |


### 2. 定性指标

- **系统理解提升**：从"知道 MLFQ 是多级队列"到"知道时间片到期时降级、on_block 时不降级、boost_all 防饥饿"这些具体实现细节。尤其是在实现 `record` 函数时，搞清楚了 Preempt（回就绪队列，累计 wait_ticks）和 Block（进阻塞态，不算等待 CPU）的本质区别。
- **工程能力提升**：学会了"策略与机制分离"的设计模式——同一套 `PluggableScheduler` trait 可以插入任意调度算法，不需要改内核其他代码。理解了"两阶段记账"（连续时间结算 + 离散事件处理）的统计方法。
- **抽象能力提升**：把 5 种调度算法统一到一个 trait 下，发现它们的本质差异只在 `pick_next` 的选择策略和 `on_tick` 的抢占判断上。
- **代价**：模拟方式虽然更容易做定量对比，但缺少真实内核中的中断/并发问题（如调度队列需要关中断保护），这部分体验不如原实验。

### 3. 与本校现有实验教程的对比分析


| 对比维度     | 原教程（ch5 stride）                  | 我的实验（exp4 调度套件）                      |
| -------- | -------------------------------- | ------------------------------------ |
| **覆盖范围** | 1 种算法（stride）                    | 5 种算法 + 可扩展                          |
| **教学结构** | 读代码 → 填实现 → 跑测试通过                | 读概念 → 统一接口 → 实现 → 定量对比 → 分析          |
| **测试体系** | 功能验收（字符串匹配）                      | 脚本化 workload + 自动输出 6 项指标            |
| **可扩展性** | 改 `fetch` 函数，耦合在 `ProcManager` 里 | 实现 `PluggableScheduler` trait 即可，零耦合 |
| **可复现性** | 依赖 QEMU + 内核编译环境                 | `cargo test` 直接跑，无环境依赖               |
| **反馈速度** | 编译内核 + 启动 QEMU + 运行用户程序（~30s）    | `cargo test`（< 1s）                   |


## 四、T2L5 学习效果评估

### 1. 定量指标

**覆盖度对比**


| 维度 | 原实验（ch8） | 我的扩展实验（exp5） |
| ---- | ---- | ---- |
| 同步原语 | MutexBlocking / Semaphore / Condvar（3 种） | SpinLock / MutexBlocking / Semaphore / Condvar / RwLock（5 种） |
| 同步接口 | `Mutex { lock, unlock }`（2 个方法） | 统一 `Mutex` trait + 各原语阻塞式 API（`acquire/release`、`read_lock/write_lock`） |
| 可观测性 | 无量化指标，只看"死锁检测能否触发" | 统一 `SyncMetrics` 采集 7 项指标 + `InstrumentedMutex` 装饰器自动记录 |
| 测试体系 | 功能验收（用户程序能否跑通） | 正向 + 反向 + 压力 + 对比共 22 个测试，脚本化可重复 |
| 经典场景 | 无 | 生产者-消费者、读者-写者、哲学家就餐 |


**性能/行为对比（exp5 实测数据）**


| 指标 | Mutex+Sem (ProdCons) | RwLock (RW) | MutexBlocking (RW) | Ordered (DinPhil) | Unordered (DinPhil) |
| ---- | ---- | ---- | ---- | ---- | ---- |
| 竞争次数 | 1359 | 374 | 759 | 4 | 75 |
| 获取次数 | 1600 | 1600 | 1600 | 500 | 12 |
| 平均等待(us) | 22.3 | 102.7 | 122.9 | 2038.0 | 13447.9 |
| 最大等待(us) | 161 | 1593 | 871 | 3410 | 202104 |
| 上下文切换 | 768 | 111 | 759 | 4 | 6 |
| 饥饿次数 | 0 | 0 | 0 | 0 | 0 |


关键结论：

- **RwLock vs MutexBlocking（读者-写者场景）**：RwLock 的竞争次数降低 51%（374 vs 759），上下文切换降低 85%（111 vs 759），因为多读者可并发进入临界区而无需排队
- **有序 vs 无序哲学家就餐**：无序模式触发死锁，仅完成 12/500 餐（2.4%）；有序模式 500 餐全部完成，平均等待时间降低 85%
- **Spin vs Sleep 对比**：轻负载（800 ops）下 SpinLock 177μs vs MutexBlocking 179μs，差异很小；但自旋锁在高竞争 + debug 模式下需要 TTAS + 退避策略才能正常工作
- 后续又补做了内核替换验证：`tg-rcore-tutorial-ch8` 已改为依赖 `exp5-sync` 的 `kernel` feature，用户态统一走 `syscall-t3l8`，并通过 `cargo check`，说明 `exp5` 已经完成了替换原 sync + 接通 syscall 的真实章节验证

**反向测试覆盖（每个原语一个"故意出错→检测超时"的对照）**


| 原语 | 故意的错误 | 检测方式 | 预期 |
| ---- | ---- | ---- | ---- |
| SpinLock | acquire 后不 release | 500ms 超时检测 | 第二个 acquire 永远卡住 |
| MutexBlocking | acquire 后不 release | 500ms 超时 + try_acquire_timeout | acquire 卡死，try 返回 false |
| Semaphore | acquire 后不 release/up | 500ms 超时 + down() 返回值 | 第二个 acquire 永远阻塞 |
| Condvar | 注册等待者后不 signal | waiting_count 检查 | 队列保持非零 |
| RwLock | write_lock 后不 unlock | 500ms 超时（分别测读者和写者） | read_lock 和 write_lock 全部卡死 |

**原实验 vs 新实验的接口对比**


| | 原 ch8 `Mutex` | 新 exp5 扩展 |
| --- | --- | --- |
| 自旋锁 | 无（仅内核 `UPIntrFreeCell`） | `SpinLock`：TTAS + 指数退避 |
| 读写锁 | 无 | `RwLock`：读者并发 / 写者独占 |
| 指标采集 | 无 | `InstrumentedMutex` 装饰器，7 项指标 |
| 阻塞式 API | 无（由内核调度器处理） | `acquire/release`：用户态直接阻塞 |
| 超时获取 | 无 | `try_acquire_timeout`：可检测死锁 |
| 公平性分析 | 无 | 变异系数（CV）+ 每线程等待时间分布 |


### 2. 定性指标

- **同步语义理解提升**：从"知道信号量就是 P/V"到理解 `release()` 中 `locked` 标志必须先释放再唤醒等待者，否则被唤醒线程看到 `locked=true` 又会重新入队形成死锁。这种在实际多线程环境下的 bug 排查经历比看教科书深刻得多。
- **硬件架构感知**：在 ARM64 (Apple Silicon) debug 模式下发现纯 CAS 自旋锁会活锁，理解了 LDXR/STXR 指令在多核缓存争抢下的特性，以及 TTAS（Test-and-Test-And-Set）+ 指数退避的必要性。
- **并发 bug 调试能力**：学会了通过"手动推演多线程时序"定位 release 的死锁 bug 和 producer-consumer 消费者终止竞态条件，以及用超时机制将"挂起"转化为可检测的测试断言。
- **设计模式**：`InstrumentedMutex<M: Mutex>` 用装饰器模式把指标采集与同步原语解耦——任何实现了 `Mutex` trait 的类型都可以被包装，不修改原始代码就能观测行为。
- **代价**：用户态模拟无法体验内核中的关中断保护和真实调度器交互（如 `make_current_blocked` / `re_enque`），对中断上下文下的同步问题（如中断处理程序中不能用睡眠锁）缺乏直接体验。

### 3. 与本校现有实验教程的对比分析


| 对比维度 | 原教程（ch8 死锁检测） | 我的实验（exp5 同步套件） |
| ---- | ---- | ---- |
| **覆盖范围** | 3 种原语（Mutex/Sem/Condvar） | 5 种原语 + InstrumentedMutex 装饰器 |
| **教学结构** | 读代码 → 实现银行家算法 → 跑通测试 | 实现原语 → 正向/反向测试 → 经典场景 → 定量对比 |
| **测试体系** | 功能验收（检测到死锁即通过） | 22 个测试：正向 5 + 并发 4 + 反向 7 + 对比 6 |
| **反向验证** | 无（只有正向"能跑通"） | 每个原语故意出错 → 超时检测 → 断言失败 |
| **可观测性** | 无量化指标 | 7 项指标 + 对比表 + 公平性 CV |
| **可复现性** | 依赖 QEMU + 内核编译环境 | `cargo test`（< 10s） |
| **反馈速度** | 编译内核 + 启动 QEMU（~30s） | `cargo test --test-threads=1`（~8s） |

## 五、T3L1 学习效果评估

### 1. 定量指标

**覆盖度对比**

| 维度 | 原教程（ch1） | 我的扩展实验（T3L1） |
| ---- | ---- | ---- |
| 输出形态 | SBI 串口文本输出 | VirtIO-GPU framebuffer 图形输出 |
| 设备路径 | `console_putchar` / `shutdown` | `console_putchar` + `Gpu::new` / `render_once` / `flush` |
| 图形数据组织 | 无 | `Piece::Tri` / `Piece::Quad` 共 13 个图元（`O` 6 块，`S` 7 块） |
| 可调参数 | 基本无 | 颜色常量 + `O_PIECES` / `S_PIECES` 顶点数组 |
| 渲染接口 | 无 | `clear` / `render_piece` / `render_pieces` / `render_progressive` / `render_tangram` |
| 运行结果 | 启动后立即结束 | QEMU 窗口持续显示静态七巧板 `OS` 图案 |

**实现规模对比**

| 项目 | 数值 |
| ---- | ---- |
| 新增图形驱动模块 | 1 个（`src/gpu.rs`） |
| 新增图形渲染模块 | 1 个（`src/tangram.rs`） |
| 七巧板总图元数 | 13 |
| 使用颜色数 | 7 |
| 图元类型 | 2 类（三角形、四边形） |

关键结论：

- 与原始 ch1 只验证“最小执行环境 + 串口输出”相比，T3L1 额外覆盖了 MMIO 设备发现、DMA/队列分配、framebuffer 写入、scanout 刷新这一整条图形输出链路。
- 该实验虽然不是动画，但已经把后续图形实验最关键的底座搭好了：图像数据结构、像素栅格化、一次性 flush，以及逐块显示可复用接口。
- 从“题目完成度”看，当前实现已经满足参考图静态显示，并且保留了后续 ch2 逐块渲染所需的最小接口。

### 2. 定性指标

- **系统理解提升**：相比单纯打印 `Hello, world!`，这次真正把“操作系统如何驱动显示硬件”走通了一遍。虽然使用的是 QEMU 提供的 VirtIO-GPU，而不是实体显卡，但我已经能把“设备发现 -> 初始化 -> 申请 framebuffer -> 写像素 -> flush 到窗口”的路径串起来理解。
- **抽象设计体会**：一开始我倾向于把七巧板做成“标准积木 + 旋转/缩放”的参数化系统，后来发现这会让题目本身被抽象过度。最后改成直接维护顶点数组之后，代码更短、更容易核对参考图，也更适合做人工微调。这让我更清楚地认识到：抽象并不是越多越好，应该为当前任务服务。
- **与 AI 协作能力提升**：T3L1 很适合暴露 AI 的边界。只要问题里混入“图像几何关系”这类信息，模型就容易给出看似合理但实际不对的结果。相比“让它一步到位”，更有效的方法是让我先决定结构和验收标准，再让 AI 实现底层函数、整理接口、修编译错误，我自己负责最终几何摆放与 review。
- **调试能力提升**：过程中遇到了 QEMU 图形后端、DMA 池大小、设备枚举位置、图形不显示等问题。相比普通算法题，这类问题更接近真实系统开发中的“环境 + 驱动 + 数据”联合调试，对我很有帮助。
- **代价**：这类实验的反馈回路比纯 `cargo test` 慢得多，需要结合 QEMU 图形窗口和串口日志一起判断；而且图案是否“像”参考图，最后仍需要人工目测，不太容易完全自动化验证。

### 3. 与本校现有实验教程的对比分析

| 对比维度 | 原教程（ch1） | 我的实验（T3L1 图形扩展） |
| ---- | ---- | ---- |
| **覆盖范围** | 裸机入口、SBI、串口输出 | 裸机入口、SBI、VirtIO-GPU、framebuffer、图元栅格化 |
| **可视化反馈** | 只有终端文本 | 有 QEMU 图形窗口，结果更直观 |
| **工程复杂度** | 较低，聚焦最小启动链路 | 更高，需要同时处理运行参数、驱动初始化和图形数据 |
| **调试方式** | 看串口输出即可 | 需要结合串口日志 + 图形窗口一起判断 |
| **可拓展性** | 适合作为后续内核实验的启动底座 | 适合作为后续图形/小游戏/逐块动画实验的显示底座 |
| **教学体验** | 强调“为什么裸机程序能跑” | 在保留启动流程教学价值的同时，增加了更强的成果感和可视化吸引力 |

总结来看，原教程更适合讲清“最小执行环境”的原理；而 T3L1 在不破坏 ch1 主线结构的前提下，把成果从“能输出字符串”提升到了“能驱动图形设备显示图案”。从学习体验上说，这种可视化的正反馈更强，也更能帮助我把抽象的启动流程、内存分配和设备驱动联系起来。

## 六、T3L2 学习效果评估

### 1. 定量指标

**覆盖度对比**

| 维度 | 原教程（ch2） | 我的扩展实验（T3L2） |
| ---- | ---- | ---- |
| 用户程序数量 | 基础测试程序 8 个 | 原有 8 个 + tangram 用户程序 13 个 |
| syscall 语义 | `write` / `exit` | `write` / `exit` + `fd = 100` 绘图协议 |
| 输出形态 | 串口日志 | 串口日志 + QEMU 图形窗口逐块动画 |
| 图形能力 | 无 | VirtIO-GPU + framebuffer + `render_progressive` |
| 向前兼容性 | 只验证基础批处理 | 原有 `./test.sh` 仍 5/5 通过 |
| 内存布局处理 | 默认用户程序基址 | 额外处理 GPU DMA/BSS 与 app base 冲突 |

**实现规模对比**

| 项目 | 数值 |
| ---- | ---- |
| 新增图形模块 | 2 个（`src/gpu.rs`、`src/tangram.rs`） |
| 新增 tangram 用户程序 | 13 个 |
| 新增绘图协议 | 1 个（`fd = 100`） |
| 复用的 ch1 渲染接口 | 4 个（`o_pieces`、`s_pieces`、`render_piece`、`render_progressive`） |
| 基础测试结果 | `./test.sh` 通过，5/5 PASS |

关键结论：

- 与原始 ch2 只覆盖“批处理 + Trap + write/exit”相比，T3L2 额外把图形设备、用户程序批量编排和渐进式渲染接进了同一套执行框架。
- 这次扩展最有价值的不是“多写了一个 syscall”，而是证明了原有 `write` 路径本身就足够承载一个轻量图形协议。
- 从实验结果看，系统既能保持原有基础测试全部通过，又能在测试程序后继续执行 13 个 tangram app，说明向前兼容目标是达成的。

### 2. 定性指标

- **系统理解提升**：我对“批处理系统”不再只停留在“顺序跑多个程序”的表面理解，而是更清楚地看到了：只要接口设计得当，批处理框架本身也可以驱动视觉效果，而不只是输出文本。
- **接口设计体会**：相比直接扩展一套新的 syscall ID，这次把绘图请求编码进 `write(fd = 100, ...)` 更贴近“最小改动完成集成”的工程思路。它说明共享接口未必需要不断膨胀，很多时候协议层约定就足够。
- **调试能力提升**：这次最典型的 bug 是 `IllegalInstruction`，但根因其实是 DMA pool / 堆 / BSS 和用户程序基址冲突。这个问题逼着我把“异常现象”和“真实根因”分开看，而不是一看到非法指令就去怀疑汇编或编译器。
- **与 AI 协作能力提升**：这次 AI 比较擅长的是“把 ch1 里的 GPU / tangram 代码搬到 ch2 并接线”，但它不太能主动意识到“不能破坏原有基础测试”这类隐含约束，需要我明确强调“向前兼容”和“保留 ch2 主线实现”。
- **代价**：图形 runner 需要图形后端，测试脚本又需要 QEMU 正常退出，所以文档、运行参数和内核结束逻辑都要兼顾“手动观察”和“自动测试”两种场景，复杂度明显比纯串口版高。

### 3. 与本校现有实验教程的对比分析

| 对比维度 | 原教程（ch2） | 我的实验（T3L2 图形批处理扩展） |
| ---- | ---- | ---- |
| **覆盖范围** | 批处理、Trap、`write/exit` syscall | 批处理、Trap、`write/exit`、VirtIO-GPU、逐块动画 |
| **用户程序角色** | 主要用于验证 syscall 与异常处理 | 同时承担“驱动动画步骤”的角色 |
| **输出反馈** | 终端文本 | 文本日志 + 图形窗口，结果更直观 |
| **接口设计** | 标准 syscall 教学示例 | 在保留标准 syscall 的前提下增加轻量协议 |
| **向前兼容要求** | 主要关注功能跑通 | 既要功能扩展，也要原基础测试不退化 |
| **工程挑战** | 上下文切换和 Trap 处理 | 上下文切换 + Trap + 图形设备 + 内存布局冲突 |

总结来看，原教程更适合讲清 ch2 的“批处理系统骨架”；而 T3L2 则在不推翻这套骨架的前提下，把它扩展成了一个能产生可视化结果的教学实验。相比单纯看到程序依次打印字符串，这种“一个 app 对应一块拼图”的结果更容易让我把批处理、syscall、设备驱动和用户程序之间的关系串起来。

## 六、T3L5 Doom 移植调试记录

这部分记录我在把 `doomgeneric` 移植到 ch8 内核过程中，已经确认过的 bug、现象和修复过程，按排查顺序整理如下。

### 1. 已定位并修复的问题

1. `I_PrintBanner()` 一进入就卡死  
根因不是 Doom 本身，而是我在 `rcore_libc.c` 里把 `memcpy/memset/memmove/memcmp/strlen/strcpy/strncpy/strcmp/strncmp` 这类函数直接写成了 `__builtin_*` 包装。对当前交叉编译器来说，这会被优化成“函数调用自己”，最后在反汇编里表现为 `j strlen` 这种无限自跳。修复方法是把这些函数全部改成手写循环实现。

2. 资源加载过程中随机出现 `W_CacheLumpNum >= numlumps`，随后又出现 `W_Read()` 的 `LoadPageFault`  
根因是我第一次手写的 `strncpy()` 有 off-by-one：当源串提前结束时，会额外多写一个字节。`lumpinfo_t` 的布局是 `name[8]` 后面紧跟 `wad_file` 指针，因此这个 bug 会直接踩坏 WAD 目录项里的文件指针，导致前面看起来像“随机 lump 越界”，后面则在 `wad->file_class->Read(...)` 处因为坏指针触发页故障。修复后，`R_Init` 能稳定跑完整个资源初始化阶段。

3. `HU_Init` 报 `W_GetNumForName: STCFN33 not found!`  
WAD 里实际存在的是 `STCFN033`。真正的问题是我自己实现的 `snprintf` 只支持宽度，不支持整数精度，所以 `DEH_snprintf(buffer, 9, "STCFN%.3d", j++)` 被错误格式化成了 `STCFN33`。修复方式是在 `do_printf()` 中补上对 `%.[0-9]+d/u/x` 的零填充精度支持。

4. 图形初始化成功后，Doom 进程立刻正常退出  
这不是崩溃，而是平台层逻辑漏写：`doomgeneric_rcore.c` 的 `main()` 只调用了 `doomgeneric_Create()`，没有像 SDL 后端那样进入 `doomgeneric_Tick()` 主循环。补上无限循环后，进程会保持常驻，图形后端也会持续刷新。

### 2. 排查过程中保留的关键记录

1. 一开始从 shell 输入 `doomgeneric` 后看起来“马上没反应”，我先怀疑 ELF 装载失败，于是在内核 `exec` 路径和 `from_elf` 中加日志，确认 Doom ELF 的 1.9MB 文件内容和约 8.7MB 的虚拟内存映射都已成功建立。

2. 为了判断是否真的进入了用户态 `main()`，我在 `doomgeneric_rcore.c` 里加了一个直接走 `SYS_write` 的 `early_puts()`，并验证了串口输出与 `printf` 都可用，从而把问题范围缩小到 Doom 初始化流程内部。

3. 之后通过在 `doomgeneric_Create()` 和 `D_DoomMain()` 中逐段打点，确认最早的卡点在 `I_PrintBanner()`；再通过反汇编发现 `strlen` 等基础 libc 符号被编译成了无限自跳，最终定位到 `__builtin_*` 这一层。

4. 修完基础字符串/内存函数以后，Doom 已经能继续走到 `R_Init`、`HU_Init`、`ST_Init` 和 `I_InitGraphics`。目前 framebuffer 初始化成功，320x200 的 Doom 画面已经能被映射到 640x400 的 VirtIO-GPU 输出上。

### 3. 当前状态

- Doom 已经可以完成 WAD 加载、渲染资源初始化、HUD/状态栏初始化和 framebuffer 初始化。
- 用户态进程不再因为基础 libc bug 崩溃，也不会在初始化完成后立刻退出。
- 已补上一个非阻塞 `input_getchar` syscall，`DG_GetKey()` 也已经接入终端输入，支持方向键转义序列以及一组终端友好的按键映射（如 `WASD` 移动、`J` 开火、`K` 交互）。
- framebuffer 提交路径从“左上角贴 640x400”改成了按整数倍居中缩放；在当前 QEMU 的 `1280x800` 显示模式下，会自动做 `2x` 放大，从而铺满整个窗口。
- 当前阶段已经从“只能启动”推进到“可以持续运行 + 有基础键盘控制 + 显示满屏”。后续如果还要继续完善，主要工作会集中在输入体验细化（例如更完整的按键映射和更真实的 press/release 语义）。

### 4. 2026-03-28 黑屏问题复盘（新增）

1. 现象  
`doomgeneric` 进程在 shell 中可启动，键盘输入有响应，但 QEMU 图形窗口持续黑屏。

2. 根因（本次新增定位）  
`ch8` 内核里的 `fb_present` 之前只对用户缓冲首地址做了一次 `address_space.translate`，随后直接按 `640*400*4` 连续读内存。这个做法默认“用户虚拟地址对应的物理页是连续的”，在实际分页场景下不成立，导致跨页后读到错误数据（常见结果就是整帧接近全黑）。

3. 修复方案  
在 `tg-rcore-tutorial-ch8/src/main.rs` 中新增按页拷贝逻辑：逐页 `translate` 用户缓冲并复制到内核 staging buffer，再把 staging buffer 提交给 `virtio_gpu::present`。这样不依赖物理页连续性，帧数据语义正确。

4. 调试增强（便于后续排障）  
- 内核 `fb_present` 新增错误日志：短缓冲、用户地址不可读、GPU 提交失败都会打印告警。
- `doomgeneric_rcore.c` 新增 `fb_get_info` 启动日志与 `fb_present` 失败日志（限次打印），用于区分“内核帧缓冲未初始化”与“提交路径异常”。

5. 关于“是否已接入原版游戏全部功能”的结论  
目前**没有**做到“原版 Doom 的所有能力都完整接入内核”。当前已接入的是：
- 文件 I/O（WAD 读取所需 open/read/lseek/fstat 路径）；
- 基础时钟与调度让步（`clock_gettime` / `sched_yield`）；
- 图形输出（`fb_present`）与串口键盘输入（`input_getchar` + 键位映射）。

仍未完整接入/对齐原版体验的部分包括：
- 鼠标输入（README 也明确说明未接）；
- 真实音频输出链路（当前移植未提供设备级声音后端）；
- 更完整的键盘按下/释放语义与高级输入设备支持（如手柄、多人网络相关能力）。

### 5. 2026-03-28 Doom demo / 交互开关与黑屏进一步定位（新增）

1. 启动模式开关  
- `initproc` 的默认启动目标改成了 `doomgeneric`，因此 `cargo run` 时会默认直接进入 Doom，而不是先停在 shell。
- `doomgeneric` 默认构建为**无交互 demo 模式**：`make -f Makefile.rcore` 会把参数固定为 `-iwad doom1.wad -playdemo demo1`，便于优先验证显示链路。
- 若要测试键盘交互，可改用 `make -f Makefile.rcore DG_MODE=interactive` 重新编译 Doom，再执行 `cargo build` / `cargo run` 重新打包镜像。

2. 这次进一步确认到的事实  
- 在 `-display none` 的无窗口 QEMU 启动中，串口日志已经能稳定看到 `[doomgeneric] mode=demo (-playdemo demo1)`，说明 Doom 的自动 demo 模式确实已经生效。
- 同时内核日志也能看到 `virtio-gpu: first present ... opaque_sample=1024/1024`，说明用户态第一帧已经成功提交到 VirtIO-GPU，且 alpha 不为 0，不是“应用根本没出图”。

3. 新结论  
如果本地 QEMU 图形窗口仍然是黑的，那么问题已经更偏向**宿主机 QEMU 图形后端显示**，而不是 guest 内核 / Doom 逻辑本身。也就是说，当前链路更像是：
- guest 侧：已初始化 GPU、已进入 Doom、已提交非透明帧；
- host 侧：窗口没有把扫描输出正确显示出来（例如 `cocoa` 后端兼容性/刷新行为问题）。

4. 后续排查建议  
优先尝试把 `tg-rcore-tutorial-ch8/.cargo/config.toml` 里的 `-display cocoa` 切换为 `-display sdl` 或 `-display gtk` 再验证；若切换后能看到 demo，则可以基本确认是 QEMU 图形后端问题，而不是内核 framebuffer 路径问题。
